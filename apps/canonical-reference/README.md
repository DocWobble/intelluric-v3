# Canonical visual reference

This executable page demonstrates the InTelluric visual foundation with semantic HTML, direct artwork, and replaceable fixture content.

Its words, labels, records, and `data-action-id` values are not product contracts. The page has no production destinations. Its appearance comes exclusively from `ui/visual-system.css`.
