# Screenshot Source Provenance

The seven binding screenshots were supplied directly in the implementation conversation on 2026-07-25. The source filenames, native dimensions, route mappings, and source SHA-256 digests are recorded in `SCREENSHOT_MANIFEST.md`.

Normalized repository fixtures preserve the full composition at reduced review resolution. Source-resolution files remain the immutable evidence used to derive the recorded digests; normalized fixtures exist so agents and visual-regression tooling can inspect the contract without relying on conversation state.
