# InTelluric Frontend Visual Authority

Status: **binding — version 3**

This file is the single entry point for frontend implementation.

## Primary contracts

| Route | Source identity | Repository transport | Native viewport | Role |
|---|---|---|---:|---|
| `/` | `contract-reference.jpeg`, SHA-256 `b7a5d2fb39c86543c0b619ac8e5c3a729cb7de6cc0a3eefb629f89628a42ecc6` | `reference/visual-contract/INTELLURIC-HOMEPAGE-OPTIMIZED-2026-07-29.preview.webp` | `1448 × 1086` | Optimized public homepage, commercial hierarchy, filtered artifact-evidence browser |
| `/pitch-synthase/wizard/reference` | `1872091A-8BC0-4130-AEB8-8CF0D244ECD2.jpeg`, SHA-256 `fc77b6444a1f5ba0caf532716cd9cda4e567f87b1d342dd117d6a86de42d42a2` | `1872091A-8BC0-4130-AEB8-8CF0D244ECD2.contract.webp` | `1448 × 1086` | Pitch Synthase instrument shell and seven-step workflow |

The homepage transport preview is a review aid. The exact source identity above remains the pixel authority. It must not be silently replaced, resized, cropped, annotated, or recompressed under the same identity.

## Authority order

1. Primary source pixels and recorded source hash.
2. `contracts/HOMEPAGE_REVISION_CONTRACT.md` for `/`.
3. `contracts/SCREEN_GEOMETRY_CONTRACT.md`.
4. `reference/visual-contract/visual-contract.v3.json`.
5. `contracts/VISUAL_SYSTEM_CONTRACT.md`.
6. `contracts/FRONTEND_ARCHITECTURE.md`.
7. `contracts/PIXEL_ACCEPTANCE_CONTRACT.md`.
8. `packages/design-tokens/src/`.
9. `packages/material-system/src/`.
10. Approved content records.

A lower authority never overrides a higher authority.

## Supersession

The former public-homepage fixture `DECF9777-2952-4B46-A439-DF4BE7F67DEF.jpeg`, its four-line due-diligence proposition, selected-work row, and separate diagnostic callout are **historical**. They no longer govern `/`.

The Pitch Synthase Step 2 primary fixture remains active and unchanged.

`visual-contract.v2.json` is historical. `visual-contract.v3.json` is the machine-readable authority.

## Homepage observable invariants

- shared InTelluric header and material grammar;
- optimized two-line proposition: `We help your idea` / `defend itself.`;
- direct actions: `Start a Project` and `View Sample Work`;
- four-cell scope strip;
- six artifact-type controls;
- one attached sample-work carousel tray;
- selecting an artifact type replaces the tray collection in place without page navigation;
- carousel controls, drag/scroll, keyboard access, and mobile continuation affordance;
- one integrated closing project-intake callout;
- no superseded selected-work row or separate diagnostic panel on the homepage;
- dense first-viewport composition; no generic marketing whitespace.

## Pitch Synthase invariants

The public header, machined instrument frame, exact seven labels, three-column Step 2 workspace, carry-forward controls, and footer rail remain governed by the prior primary Pitch Synthase source and current v3 machine contract.

## Asset boundary

Whole-page screenshots are regression evidence, never runtime page backgrounds. Owl art, example thumbnails, uploaded references, and other bounded content imagery may be raster assets. Navigation, headings, buttons, selectors, cards, carousel controls, labels, frames, and fields remain semantic interface elements.
