# InTelluric Frontend Visual Authority

Status: **binding — version 3**

This file is the single entry point for frontend implementation.

## Primary contracts

| Route | Visual reference | Executable source | Native viewport | Role |
|---|---|---|---:|---|
| `/` | supplied `contract-reference.jpeg`, recorded SHA-256 `b7a5d2fb39c86543c0b619ac8e5c3a729cb7de6cc0a3eefb629f89628a42ecc6` | `staging/optimized-homepage-2026-07-29/` plus `TEXT_SOURCE_MANIFEST.md` | `1448 × 1086` | Optimized public homepage, commercial hierarchy, filtered artifact-evidence browser |
| `/pitch-synthase/wizard/reference` | `1872091A-8BC0-4130-AEB8-8CF0D244ECD2.jpeg`, SHA-256 `fc77b6444a1f5ba0caf532716cd9cda4e567f87b1d342dd117d6a86de42d42a2` | preserved v2 instrument contracts and transport fixture | `1448 × 1086` | Pitch Synthase instrument shell and seven-step workflow |

The homepage screenshot is the human visual reference. It does not have to be stored as a repository binary. The extracted HTML, CSS, JavaScript, package metadata, and implementation notes are the repository-resident executable contract.

## Authority order

1. Supplied primary screenshot as the visual reference.
2. `contracts/HOMEPAGE_REVISION_CONTRACT.md` for `/`.
3. `staging/optimized-homepage-2026-07-29/TEXT_SOURCE_MANIFEST.md` and its verified text files.
4. `contracts/BASELINE_SCOPE.md` for unchanged Pitch Synthase and shared-system obligations preserved from v2.
5. `contracts/SCREEN_GEOMETRY_CONTRACT.md`.
6. `reference/visual-contract/visual-contract.v3.json`.
7. `contracts/VISUAL_SYSTEM_CONTRACT.md`.
8. `contracts/FRONTEND_ARCHITECTURE.md`.
9. `contracts/PIXEL_ACCEPTANCE_CONTRACT.md`.
10. `packages/design-tokens/src/`.
11. `packages/material-system/src/`.
12. Approved content records.

A lower authority never overrides a higher authority.

## Supersession

The former public-homepage fixture `DECF9777-2952-4B46-A439-DF4BE7F67DEF.jpeg`, its four-line due-diligence proposition, selected-work row, and separate diagnostic callout are historical. They no longer govern `/`.

The complete former contract state is retained under `contracts/baselines/v2/`. `contracts/BASELINE_SCOPE.md` identifies the unchanged Pitch Synthase and shared-system sections that remain binding. This preserves detail without creating a second homepage authority.

`visual-contract.v2.json` is a historical pointer. `visual-contract.v3.json` is the machine-readable current authority.

## Homepage observable invariants

- shared InTelluric header and material grammar;
- optimized two-line proposition: `We help your idea` / `defend itself.`;
- direct actions: `Start a Project` and `View Sample Work`;
- four-cell scope strip;
- six artifact-type controls;
- one attached sample-work carousel tray;
- selecting an artifact type replaces the tray collection in place without page navigation;
- carousel controls, drag/scroll, keyboard access, and mobile continuation affordance;
- one integrated closing project-intake callout;
- no superseded selected-work row or separate diagnostic panel on the homepage;
- dense first-viewport composition; no generic marketing whitespace.

## Pitch Synthase invariants

The public header continuity, machined instrument frame, exact seven labels, component/state model, backend-DAG ownership, three-column Step 2 workspace, carry-forward controls, footer rail, responsive rules, and acceptance requirements remain governed by v3 plus the scoped v2 baseline.

## Asset boundary

Whole-page screenshots are reference evidence, never runtime page backgrounds. Owl art, example thumbnails, uploaded references, and other bounded content imagery may be replaced independently. Navigation, headings, buttons, selectors, cards, carousel controls, labels, frames, and fields remain semantic interface elements.
